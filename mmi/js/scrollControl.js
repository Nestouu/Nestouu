// Désactiver le défilement
function disableScroll() {
    document.body.style.overflow = 'hidden';
    }

  // Réactiver le défilement
    function enableScroll() {
        document.body.style.overflow = '';
    }

  // Appeler la fonction pour désactiver le scroll
    disableScroll();
